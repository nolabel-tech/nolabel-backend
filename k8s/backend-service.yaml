apiVersion: v1
kind: Service
metadata:
  name: python-app-service
  namespace: nolabel-dev
  annotations:
    traefik.mesh.traefik.io/traffic-type: "HTTP"
spec:
  type: ClusterIP
  selector:
    app: python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
